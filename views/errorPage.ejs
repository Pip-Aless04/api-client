<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            text-align: center;
        }
        .error-code {
            font-size: 8rem;
            font-weight: bold;
            margin-bottom: 1rem;
            opacity: 0;
            transform: translateY(-20px);
            transition: opacity 0.5s, transform 0.5s;
        }
        .error-code span { color: #40E0D0; }
        .message {
            font-size: 1.5rem;
            color: #FF8C42;
            margin-bottom: 2rem;
        }
        .back-button {
            background: linear-gradient(45deg, #40E0D0, #3B82F6);
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 1rem;
            border-radius: 25px;
            cursor: pointer;
            transition: transform 0.3s;
        }
        .back-button:hover {
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="error-code">
            <span><%= errorCode[0] %></span><span><%= errorCode[1] %></span><span><%= errorCode[2] %></span>
        </div>
        <div class="message"><%= message %></div>
        <button class="back-button" onclick="goBack()"><%= backButtonText %></button>
    </div>

    <script>
        window.onload = function() {
            setTimeout(() => {
                const errorCode = document.querySelector('.error-code');
                errorCode.style.opacity = '1';
                errorCode.style.transform = 'translateY(0)';
            }, 100);
        }

        function goBack() {
            window.location.href = '<%= backButtonHref %>';
        }
    </script>
</body>
</html>
